[tools]
cmake = "4.1.1"
go = "1.26.0"
"go:golang.org/x/tools/gopls" = "0.21.0"
"ubi:lucasew/workspaced" = "0.3.4"

[tasks.install]
depends = ["install:*"]

[tasks."install:go"]
run = "go mod download"

[tasks.test]
depends = ["test:*"]

[tasks."test:go"]
run = "go test ./..."

[tasks.codegen]
depends = ["codegen:*"]

[tasks."codegen:go"]
run = "go generate ./..."

[tasks.lint]
run = "workspaced codebase lint"

[tasks.fmt]
run = "workspaced codebase format"

[tasks.ci]
depends = ["lint", "test"]
